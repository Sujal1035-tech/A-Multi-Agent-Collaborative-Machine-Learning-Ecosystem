# Debugging target_col KeyError
- [ ] Check `agents/model/handler.py` to see why it crashes on `df.drop(target_col, axis=1)`
- [ ] Check if `target_col` can be matched case-insensitively across the pipeline.
- [ ] Implement fix or instruct user.
